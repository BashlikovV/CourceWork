unit Window_SrchOutput;

interface

uses
  Winapi.Windows, Winapi.Messages, System.SysUtils, System.Variants, System.Classes, Vcl.Graphics,
  Vcl.Controls, Vcl.Forms, Vcl.Dialogs, Vcl.StdCtrls, Vcl.ExtCtrls, Algorithms;

type
  TfrmSrchOutput = class(TForm)
    pnlTopPanel: TPanel;
    lblTopLabel: TLabel;
    procedure FormShow(Sender: TObject);
    procedure SetClose(FClose: Boolean);
    property Close: Boolean read FClose write SetClose;
  private
    { Private declarations }
  public
    { Public declarations }
    Info: TInfo;
    FClose: Boolean;
  end;

var
  frmSrchOutput: TfrmSrchOutput;

implementation

{$R *.dfm}

{ TForm1 }

procedure TfrmSrchOutput.FormShow(Sender: TObject);
const
  NL = #10#13;

var
  ResString: string;

begin
  Info.PatchString := NL + Info.PatchString;
  ResString := 'Найденный путь: %s' + NL + NL;
  ResString := ResString + 'Количество дуг в пути : %d' + NL;
  ResString := ResString + 'Длина найденного пути: %d' + NL;
  ResString := ResString + 'Количество посещений: %d' + NL;

  with Info do
  begin
    lblTopLabel.Caption := Format(ResString, [PatchString, ArcsCount, Distans,
      VisitsCount]);
  end;

end;

end.
