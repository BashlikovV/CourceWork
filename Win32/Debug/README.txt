# CourceWork

This application is intended only for constructing oriented weighted graphs with positive arc weights. Loops and multiple arcs are also not allowed. 
The application also allows you to perform one of the selected search algorithms (Dijkstra, depth, width).